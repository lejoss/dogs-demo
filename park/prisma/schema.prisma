generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model dog {
  id   Int    @id
  name String
}

model park {
  id   Int    @id
  name String
}

model user {
  id           Int    @id @default(autoincrement())
  subscription String
}
